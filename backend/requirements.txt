# Runtime deps for the backend service only (not the full dev environment's
# requirements.txt at the repo root, which also covers locate-anything.cpp's
# own Python scripts for model conversion/testing -- torch/transformers/etc.
# are not needed to serve /detect, which talks to the model via the compiled
# liblocate_anything.so, not Python).
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
Pillow>=10.0

# LA_INFERENCE_BACKEND=triton support (backend/triton_engine.py) -- talks to
# a remote Triton Inference Server, no local GPU/CUDA needed on this side.
tritonclient[http]>=2.40
numpy>=1.26
